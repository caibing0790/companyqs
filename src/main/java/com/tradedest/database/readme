Level 1: 实现基本的CRUD操作
首先，我们定义一个简单的接口和其实现类来支持数据的基本操作：创建(Create)、读取(Read)、更新(Update)和删除(Delete)。
Level 2: 操作记录与统计
接下来，在每次进行CRUD操作时，我们需要记录这些操作，并能够返回执行次数最多的前N个操作。
Level 3: 用户锁支持
为了支持用户锁定某些数据，我们需要引入一种机制来管理锁。